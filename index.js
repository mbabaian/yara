/* TO DO:
 - API calls to DPLA or some other library org
 - responses to requests that don't make sense
 */

module.exports = function(bp) {
  bp.middlewares.load()

  // bp.hear({text: 'hello'}, (event, next) => {
  //   bp.messenger.sendText(event.user.id, "Hello there! I am Monica's library assistant. How may I help you?");
  // })

  // bp.hear({text: 'hi'}, (event, next) => {
  //   bp.messenger.sendText(event.user.id, "Hello there! I am Monica's library assistant. How may I help you?");
  // })

  // bp.hear({text: 'hey'}, (event, next) => {
  //   bp.messenger.sendText(event.user.id, "Hello there! I am Monica's library assistant. How may I help you?");
  // })

  // bp.hear({text: 'fuck u'}, (event, next) => {
  //   bp.messenger.sendText(event.user.id, "That's not very nice. I don't think I want to talk to you anymore. Goodbye.");
  // })

  // bp.hear({text: 'I need a book'}, (event, next) => {
  //   bp.messenger.sendText(event.user.id, "I can help you with that. Would you like fiction or nonfiction?");
  // })

  
  // bp.hear({text: 'fiction'}, (event, next) => {
  //   bp.messenger.sendText(event.user.id, 'One moment while I search for fiction books . . .');
//})
  

 
  // bp.hear({text: 'nonfiction'}, (event, next) => {
  //   bp.messenger.sendText(event.user.id, 'One moment while I search for nonfiction books . . .');
  // })


  

}

 
